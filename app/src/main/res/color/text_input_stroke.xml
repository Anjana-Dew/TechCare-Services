<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Focused -->
    <item android:color="@color/amber_500" android:state_focused="true"/>
    <!-- Hovered -->
    <item android:color="@color/amber_500" android:state_hovered="true"/>
    <!-- Default (not focused) -->
    <item android:color="@color/teal_500"/>
</selector>